# Core deps (installed by pip; see scripts/setup_colab.sh for the one-shot path).
torch>=2.0
torchvision>=0.15
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
PyYAML>=6.0

# Optional: DCS environments require MuJoCo/EGL rendering and a separate install.
# See README -> "dcs_wrapper.py / dm_control / distracting_control license note".
# dm-control>=1.0.29
# distracting-control>=0.0.1

# Dev/test
pytest>=7.0